# Copy this file to .env and replace the value with your real key for local testing.
# Do NOT commit .env to version control.
OPENAI_API_KEY=sk-REPLACE_WITH_YOUR_KEY
# Optional: override default model (project uses gpt-4o by default)
MODEL=gpt-4o
